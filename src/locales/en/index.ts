import { Namespace } from "@locales/types";
import common from "./common";
import home from "./home";
import about from "./about";

const en: Namespace = {
  common,
  home,
  about,
};

export default en;
